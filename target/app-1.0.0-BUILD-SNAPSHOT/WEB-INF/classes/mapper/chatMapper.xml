<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.firstSpring.dao.order.ChatMapper">

    <select id="selectMsgData" parameterType="String" resultType="ChatDto">
        SELECT role_data,
               content_data
        FROM msg_data
        WHERE cust_id = #{cust_id}
        ORDER BY msg_no ASC
    </select>

    <select id="selectMsgNo" resultType="int">
        SELECT msg_no
        FROM msg_data
        ORDER BY msg_no DESC LIMIT 1;
    </select>

</mapper>